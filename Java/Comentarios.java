public class Comentarios {
    //tipos de variables en javascript -> boolean, number, string, array, object

        /*tipos de variables de java -> boolean, numero = number de javascript( int, double(16 digitos despues de la coma) y float(7 digitos despues de la coma)) ,
         String(toma cadenas de texto y se ocupa comillas "") y char(toma solo un caracter= y se ocupa comilla '1') , array, object */
        
        // Como se declaran las variables en java
        //int numero = 1;
        //String palabra = "palabra";
        //no se pueden cambiar las variables
        //[1,5,9,6,3] || ["lapiz","cuaderno","estuche","cargador"]
        // definiendo array y object
        //matriz -> [1,'a',"palabra",4]
        // arreglos -> [1,2,5,4] -> arreglos[1] == 2
        //arreglo de arreglos -> [[1,3,2],[5,8,6],[7,9,8]]
        //diccionarios -> {id:1, nombre:"nicolas"} -> diccionarios[0] == {id:1, nombre:"nicolas"}
        //diccionarios -> [{id:1, nombre:"nicolas"}, {id:1, nombre:"kiara"}]
        //diccionarios[0].nombre -> "nicolas" , diccionarios[1].id -> 2

        //List compras = *sentencia que trae datos de la base*;

        //---------------------------------------//
        /* como definimos los valores de una declaracion de usuario?
         * id -> podria ser alfanumerico, en caso de ser seria string, en caso contrario seria int
         * nombre -> string 
         * mail -> string
         * password -> string o int
         * edad -> int 
         * genero -> char 
         * 
         */

        // Declaración de variables
        /*la variable, el nombre de la variable, y el valor a asignar.
         * int nombre = 15;
         * String palabra = "este es un string";
         * Double decimal = 1.2354;
         * 
         * 
         */

         // Parsear variables (cambiar tipo)
        /*
          * ParseDouble() -> pasar desde string a Double
          *ParseInt() ->pasar desde String  a Int
          * double altura =  1.5; pasando a entero
          * int alt = (int) altura;
          *String num = 12;
          *int nuevoNum = Integer.parseInt(num);
          */
}
